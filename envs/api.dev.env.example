# Copie para api.dev.env e preencha os valores.
# Uso: docker-compose carrega envs/api.dev.env

# Obrigatório se LLM_PROVIDER ou EMBEDDING_PROVIDER = openai
OPENAI_API_KEY=

# Provedores: openai | ollama
LLM_PROVIDER=openai
EMBEDDING_PROVIDER=openai

# OpenAI (opcionais)
OPENAI_LLM_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Ollama (opcionais; use host.docker.internal se API rodar no Docker)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_LLM_MODEL=llama3.2:3b
OLLAMA_EMBEDDING_MODEL=nomic-embed-text

# RAG (opcionais)
CHUNK_SIZE=1000
CHUNK_OVERLAP=150
TOP_K=10
